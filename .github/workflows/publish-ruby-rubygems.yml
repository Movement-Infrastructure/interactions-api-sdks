name: Publish Ruby SDK to RubyGems

# Publishes sdks/ruby/v1 to RubyGems on push to `main`. Promoting develop to
# main is what releases.
#
# Publishes the version in `gemVersion` as a final release, so `gem install
# ddx_interactions_api` resolves it with no `--pre`. RubyGems refuses to re-push
# a version and `yank` does not free the number, so each release needs a
# `gemVersion` bump first. generate-ruby-sdk.yml does that on every sync PR.
#
# Auth is OIDC trusted publishing. There is no token to leak or rotate.
# RubyGems binds that publisher to this file's name, so renaming the workflow
# breaks publishing until the publisher config is updated to match.

on:
  push:
    branches: [main]
    paths:
      - 'sdks/ruby/v1/**'
  # Re-runs a publish without a new promotion, after fixing a registry-side
  # problem. It cannot republish a version that already exists.
  workflow_dispatch:

# One publish at a time. Two overlapping runs could race on the same version.
concurrency:
  group: publish-ruby-rubygems
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out release branch
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'

      - name: Verify the generated SDK is present
        run: |
          set -euo pipefail

          if [ ! -f sdks/ruby/v1/ddx_interactions_api.gemspec ]; then
            echo "::error::No gemspec in sdks/ruby/v1."
            echo "::error::The generated SDK has not been committed yet; nothing to publish."
            exit 1
          fi

      # The generator writes version.rb from `gemVersion`, so the two agreeing is
      # the signal that the SDK was regenerated after the last bump. If they have
      # drifted, the gem would publish under a version nobody reviewed.
      - name: Resolve the release version
        run: |
          set -euo pipefail

          base=$(sed -nE 's/^gemVersion:[[:space:]]*"?([^"[:space:]#]+)"?.*/\1/p' \
                 sdks/ruby/v1/openapi-generator-config.yaml)

          if [ -z "$base" ]; then
            echo "::error::Could not read gemVersion from openapi-generator-config.yaml."
            exit 1
          fi

          rb=$(sed -nE "s/^[[:space:]]*VERSION = '([^']+)'.*/\1/p" \
               sdks/ruby/v1/lib/ddx_interactions_api/version.rb)

          if [ "$base" != "$rb" ]; then
            echo "::error::gemVersion is ${base} but version.rb is ${rb}."
            echo "::error::Regenerate the SDK so the two agree before promoting."
            exit 1
          fi

          echo "GEM_VERSION=${base}" >> "$GITHUB_ENV"
          echo "Publishing ${base}."

      # RubyGems answers a re-push with a 409 that reads like a permissions
      # problem. Failing here instead names the actual fix.
      - name: Refuse a version that is already published
        run: |
          set -euo pipefail

          code=$(curl -s -o /dev/null -w '%{http_code}' \
                 "https://rubygems.org/api/v2/rubygems/ddx_interactions_api/versions/${GEM_VERSION}.json")

          if [ "$code" = "200" ]; then
            echo "::error::${GEM_VERSION} is already published. Bump gemVersion in"
            echo "::error::sdks/ruby/v1/openapi-generator-config.yaml and regenerate."
            exit 1
          fi

          if [ "$code" != "404" ]; then
            echo "::error::Unexpected HTTP ${code} checking whether ${GEM_VERSION} exists."
            exit 1
          fi

          echo "${GEM_VERSION} is unpublished."

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # README.md ships inside the gem. The PR check sees a regeneration,
      # this sees the bytes about to be uploaded.
      - name: Verify README before publishing
        run: python scripts/verify_readme.py sdks/ruby/v1/README.md --language ruby

      - name: Build the gem
        working-directory: sdks/ruby/v1
        run: gem build ddx_interactions_api.gemspec -o "ddx_interactions_api-${GEM_VERSION}.gem"

      # Package contents are permanent once pushed; yanking does not
      # unpublish them. Enumerating makes a bad s.files glob visible here
      # rather than on the registry. See docs/publish-leak-audit-checklist.md.
      - name: Enumerate artifact contents
        working-directory: sdks/ruby/v1
        run: |
          set -euo pipefail

          {
            echo "## Published artifact contents"
            echo "### gem: ddx_interactions_api-${GEM_VERSION}.gem"
            echo '```'
            tar -xOf "ddx_interactions_api-${GEM_VERSION}.gem" data.tar.gz | tar -tzf - | sort
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      # Reads the built artifact, not the gemspec source, since that is what
      # gets published.
      - name: Check gem metadata
        working-directory: sdks/ruby/v1
        run: |
          set -euo pipefail

          ruby -rrubygems/package -e '
            spec = Gem::Package.new(ARGV[0]).spec
            fails = 0
            check = lambda do |cond, msg|
              if cond
                puts "::error::#{msg}"
                fails += 1
              end
            end

            check.call(spec.authors.any? { |a| a =~ /OpenAPI[- ]Generator/i },
                       "Author is the generator default. Set gemAuthor in openapi-generator-config.yaml.")
            check.call(spec.email.to_a.any? { |e| e =~ /openapitools\.org/i },
                       "Author email is the openapi-generator project address. Set gemAuthorEmail.")
            check.call(spec.license.to_s.empty? || spec.license =~ /\Aunlicense\z/i,
                       "License is unset or the generator default. Set gemLicense.")
            check.call(spec.version.to_s.start_with?("0.0.0"),
                       "Version is the 0.0.0 placeholder; the version bump did not run.")
            check.call(spec.homepage.to_s.empty?,
                       "Homepage is unset. Set gemHomepage.")
            check.call(spec.version.prerelease?,
                       "Version #{spec.version} is a prerelease. This workflow publishes final releases.")

            if fails.zero?
              puts "Metadata checks passed: #{spec.name} #{spec.version} (#{spec.license})"
            else
              puts "::error::Refusing to publish with generated-default metadata."
              exit 1
            end
          ' "ddx_interactions_api-${GEM_VERSION}.gem"

      - name: Upload gem artifact
        uses: actions/upload-artifact@v4
        with:
          name: ruby-sdk-gem
          path: sdks/ruby/v1/*.gem
          if-no-files-found: error

  publish:
    needs: build
    runs-on: ubuntu-latest

    # Keeps publishing off arbitrary branches, and is where a required
    # reviewer attaches.
    environment:
      name: rubygems
      url: https://rubygems.org/gems/ddx_interactions_api

    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download gem artifact
        uses: actions/download-artifact@v4
        with:
          name: ruby-sdk-gem
          path: dist/

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'

      - name: Configure RubyGems credentials
        uses: rubygems/configure-rubygems-credentials@v2.1.0

      - name: Push to RubyGems
        run: |
          set -euo pipefail
          gem push dist/*.gem
