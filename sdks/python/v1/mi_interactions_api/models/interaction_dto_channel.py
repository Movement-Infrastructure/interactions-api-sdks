# coding: utf-8

"""
    Interactions API

    Interactions API documentation [test marker: MIG-1916 sync trigger]

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from mi_interactions_api.models.address_channel_details import AddressChannelDetails
from mi_interactions_api.models.dynamic_channel_details import DynamicChannelDetails
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

INTERACTIONDTOCHANNEL_ONE_OF_SCHEMAS = ["AddressChannelDetails", "DynamicChannelDetails"]

class InteractionDtoChannel(BaseModel):
    """
    Channel-of-contact details used during the outreach attempt.  If the `type` is recognized as an Address Channel (type = \"address\"), the `value` will be deserialized to that object type.  Other string-based types have specific validation documented in the \"Channel\" section below, like \"phone_number\".  If the `type` is unrecognized, the payload will be deserialized to an unvalidated, loosely typed object for flexibility.
    """
    # data type: AddressChannelDetails
    oneof_schema_1_validator: Optional[AddressChannelDetails] = None
    # data type: DynamicChannelDetails
    oneof_schema_2_validator: Optional[DynamicChannelDetails] = None
    actual_instance: Optional[Union[AddressChannelDetails, DynamicChannelDetails]] = None
    one_of_schemas: Set[str] = { "AddressChannelDetails", "DynamicChannelDetails" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    discriminator_value_class_map: Dict[str, str] = {
    }

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        if v is None:
            return v

        instance = InteractionDtoChannel.model_construct()
        error_messages = []
        match = 0
        # validate data type: AddressChannelDetails
        if not isinstance(v, AddressChannelDetails):
            error_messages.append(f"Error! Input type `{type(v)}` is not `AddressChannelDetails`")
        else:
            match += 1
        # validate data type: DynamicChannelDetails
        if not isinstance(v, DynamicChannelDetails):
            error_messages.append(f"Error! Input type `{type(v)}` is not `DynamicChannelDetails`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in InteractionDtoChannel with oneOf schemas: AddressChannelDetails, DynamicChannelDetails. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in InteractionDtoChannel with oneOf schemas: AddressChannelDetails, DynamicChannelDetails. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: Optional[str]) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        if json_str is None:
            return instance

        error_messages = []
        match = 0

        # deserialize data into AddressChannelDetails
        try:
            instance.actual_instance = AddressChannelDetails.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into DynamicChannelDetails
        try:
            instance.actual_instance = DynamicChannelDetails.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into InteractionDtoChannel with oneOf schemas: AddressChannelDetails, DynamicChannelDetails. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into InteractionDtoChannel with oneOf schemas: AddressChannelDetails, DynamicChannelDetails. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], AddressChannelDetails, DynamicChannelDetails]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


